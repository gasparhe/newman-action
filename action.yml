name: 'Newman - Postman.'
description: 'Run a private Postman collection parsing the collection ID, the environment ID, and the API key. A Fork from https://github.com/matt-ball/newman-action' 
author: 'gasparhe'
branding:
  icon: 'list'
  color: 'yellow'
inputs:
  collection:
    description: 'Collection to use'
    required: true
  environment:
    description: 'Environment to use'
    required: true
  apiKey:
    description: 'Postman API key'
    required: true
runs:
  using: 'node16'
  main: 'dist/index.js'
